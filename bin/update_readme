#!/bin/bash

if [[ "$BRANCH" == "master" ]]; then
	dir=`git rev-parse --show-toplevel`

	cat > ${dir}/README.md << "EOL"
# Rezepte

traditionsreiche Rezepte der Familie Wess

EOL


	#  loop through dir
	for d in ${dir}/*.md; do
		name=$(basename ${d%.md})
		if [ "$name" != "README" ]
		then
			printf "* [${name}](https://github.com/wessm/Rezepte/tree/master/${name}.md/)\n\n" >> ${dir}/README.md
		fi
	done

	printf "\n\nTo automatically rebuild the README file on each commit, run "\""bin/activate_hook"\"" from inside the repo once." >> ${dir}/README.md

	echo "README updated!"
fi
